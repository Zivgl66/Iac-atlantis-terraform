version: 3
projects:
  - dir: .
    autoplan:
      when_modified: ["*.tf", "*.tfvars", "*.tfstate"]
workflows:
  default:
    plan:
      steps:
        - init
        - plan
    apply:
      steps:
        - apply
