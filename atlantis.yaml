version: 3
projects:
  - dir: .
    autoplan:
      when_modified: ["*.tf", "*.tfvars", "*.tfstate"]
      enabled: true
    apply_requirements: [approved]
    workflow: default
workflows:
  default:
    plan:
      steps:
        - init
        - plan
    apply:
      steps:
        - apply
